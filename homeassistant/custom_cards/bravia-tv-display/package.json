{
  "name": "bravia-tv-display",
  "version": "1.0.0",
  "description": "Home Assistant Lovelace card: TV illustration with live ADB screenshot overlay",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "preview:card": "npm run build && npx --yes serve -p 5010 ."
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
