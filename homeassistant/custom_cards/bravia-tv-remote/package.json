{
  "name": "bravia-tv-remote",
  "version": "1.0.0",
  "description": "Home Assistant Lovelace card: Full Sony Bravia remote control with ADB",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "preview:card": "npm run build && npx --yes serve -p 5012 ."
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
