{
  "name": "pi-temp-card",
  "version": "1.0.0",
  "description": "Home Assistant Lovelace card: simple color-coded Raspberry Pi CPU temperature monitor",
  "type": "module",
  "scripts": {
    "build": "vite build && node -e \"const fs=require('fs');const h=fs.readFileSync('index.html','utf8').replace('./src/pi-temp-card.ts','./pi-temp-card.js');fs.writeFileSync('dist/index.html',h);\"",
    "preview": "vite"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
